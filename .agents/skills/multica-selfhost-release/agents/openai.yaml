interface:
  display_name: "Multica Selfhost Release"
  short_description: "Choose upstream sync, deploy, and package Multica"
  default_prompt: "Use $multica-selfhost-release to choose merge or rebase for the current branch, deploy it through my-mini to dj, update both CLIs, package the macOS app, and verify the release."
