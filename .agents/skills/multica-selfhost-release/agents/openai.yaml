interface:
  display_name: "Multica Selfhost Release"
  short_description: "Deploy, package, and install self-hosted Multica"
  default_prompt: "Use $multica-selfhost-release to deploy the current Multica branch, build the macOS DMG, and replace the local app."
